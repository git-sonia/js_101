//SET new array
//SET index of first array to 0
//SET index of second array to 0
//WHILE both indices are within range
  //push element at the index of first array to new array
  //push element at index of second array
  //increment both indicies
//return new array

merge([1, 2, 3], [4, 5, 6]); // => [1, 4, 2, 5, 3, 6]

